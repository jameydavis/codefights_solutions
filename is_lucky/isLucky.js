export function isLucky(n) {
    var halfLength = n.toString().length / 2;
    var firstHalf = 0;

    for(var i = 0; i < halfLength; i++) {
        firstHalf += n[i];
        console.log
    }

    return firstHalf;
}
